server:
  port: 8080

spring:
  application:
    name: claim-assist-backend
  datasource:
    url: jdbc:h2:mem:claimdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console