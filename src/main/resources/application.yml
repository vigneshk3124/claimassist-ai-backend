server:
  port: 8080

spring:
  application:
    name: claim-assist-backend
  datasource:
    url: jdbc:mysql://localhost:3306/claimdb?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
#  h2:
#    console:
#      enabled: true
#      path: /h2-console